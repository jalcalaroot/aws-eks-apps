apiVersion: v1
kind: Service
metadata:
  name: podinfo
  annotations:
    checkov.io/skip1: "CKV_K8S_21=mismo motivo que deployment.yaml - namespace default a proposito hasta tener un Fargate Profile dedicado en aws-eks-cluster"
spec:
  type: ClusterIP
  selector:
    app: podinfo
  ports:
    - name: http
      port: 80
      targetPort: http
